# 阿在 Token Pet V1 首轮验证记录

## 基本信息

- 验证日期：2026-04-08
- 验证人：Codex
- 设备：当前开发机
- 系统版本：macOS
- 宿主环境：Codex + 本地桌面 companion
- 插件版本：`v0.1.0-dev-preview` 准备阶段
- 是否首次安装：否，基于当前持续开发中的运行态验证

## 总体结论

- 总结论：`有条件通过`
- 是否可进入小范围私测：`暂不建议立即发`
- 本轮最关键问题：
  - 首次诞生的迎接感还可以继续补强
  - 拍准 / 拍偏 / 错过的强弱动作还可以再拉开一档
  - 单实例和桌面可见状态需要再做一轮人工桌面验证
- 本轮最关键亮点：
  - `Token -> 粮食 -> 吃 -> 能量 -> 拍 -> 成长` 主链路已经基本成立
  - 初始化身份、认主首拍、关系反馈、气泡状态样式已经形成一套完整体验
  - 发布配套文档、关闭与重置脚本已经补齐

## 一、安装与启动

### 1. 最小启动路径

- 结果：`待验证`
- 观察：
  - `launch_desktop_pet.command`、原生二进制和运行态文件均存在
  - 当前开发机上 bridge 可稳定运行
  - 已多次完成编译与重启
- 问题：
  - 还缺一轮“按 README 从零启动”的完整人工桌面验证

### 2. 关闭与重置

- 结果：`通过`
- 观察：
  - 已补 `scripts/stop_desktop_pet.command`
  - 已补 `scripts/reset_pet_data.command`
  - 两个脚本已有执行权限
- 问题：
  - 重置不会自动备份，需要在私测说明里继续强调

### 3. 自启路径

- 结果：`待验证`
- 观察：
  - `install_autostart.command` 与 `uninstall_autostart.command` 已存在
- 问题：
  - 还缺一轮安装后重启系统级验证

## 二、首次诞生

### 4. 第一眼不是默认宠物

- 结果：`通过`
- 观察：
  - 当前宠物已具备名字、灵魂描述、说话口吻、外观倾向、第一印象和出生叙事
  - 当前状态文件中可见 `petName: 阿砾搭`、`soulSignature`、`speakingStyle`、`appearanceHint`、`firstImpression`、`birthNarrative`
- 问题：
  - 首次桌面迎接感仍可再加强

### 5. 认主第一下成立

- 结果：`通过`
- 观察：
  - 当前状态文件中 `firstBondAt` 已存在
  - 认主首拍已有专属 `bond` 冲击反馈和独立完成演出
- 问题：
  - 还可继续补一层更完整的首次迎接文案或演出节奏

## 三、真实喂养

### 6. Codex Token 转粮

- 结果：`通过`
- 观察：
  - `codex-bridge-state.json` 已初始化，并持续记录 `last_total_tokens`
  - `pet-state.json` 中可见最新转粮摘要：
    - `140220 Token -> 特制机能肉粒×4 / 11217 粮值`
  - 粮仓库存和已消费事件都在持续更新
- 问题：
  - 当前验证主要基于本机开发流量，还缺一轮更干净的私测环境验证

### 7. 自动吃粮链路

- 结果：`通过`
- 观察：
  - `pet-state.json` 中可见粮仓、自动吃粮后状态变化、最近动态记录
  - 当前活动和最近日志已体现自动行为，如补眠、整理、自动吃粮后的状态变化
- 问题：
  - 还可补一轮“高质量任务餐”和“低状态自动补粮”的人工观察记录

## 四、互动与关系

### 8. 单一动作“拍一下”

- 结果：`通过`
- 观察：
  - 交互已收敛成一个动作
  - 当前状态里可见 `gentleImpactCount / mediumImpactCount / heavyImpactCount`
  - 互动能量字段已稳定存在
- 问题：
  - 无

### 9. 拍准 / 拍偏 / 错过差异

- 结果：`有条件通过`
- 观察：
  - 当前已具备：
    - 姿态差异
    - 小特效差异
    - 气泡条、边框、尾巴偏移和标签差异
    - 认主首拍独立演出
- 问题：
  - 强弱层级还能再拉开一档
  - 需要一轮纯桌面视角人工确认“无需读文字也能稳定分辨”

### 10. 关系会留下痕迹

- 结果：`通过`
- 观察：
  - 当前状态文件已记录拍击历史、关系能量、称号倾向和生成特征
  - `affinityReleaseCount`、`agitationReleaseCount`、不同 impact count 已持续积累
- 问题：
  - 还可以在图鉴中把关系痕迹再表达得更直观

## 五、独特成长

### 11. 非固定模板感

- 结果：`通过`
- 观察：
  - 当前已形成“大阶段 + 特征生成 + 关系塑形”的结构
  - 外观、偏好、称号和习惯并非单纯固定模板
- 问题：
  - 还可继续拉开不同养法带来的身体差异

### 12. AI 角色边界

- 结果：`通过`
- 观察：
  - 当前文档和实现都已固定：
    - 规则系统负责生长
    - AI 负责“它是谁”
  - 主循环不依赖每次实时 AI 调用
- 问题：
  - 后续真实 AI 初始化接入时仍需守住边界

## 六、稳定性

### 13. 单实例稳定

- 结果：`待验证`
- 观察：
  - 启动脚本和应用内均已具备单实例保护
  - 但本轮 `ps` 抓取中，桌宠进程表现仍需要再做一轮人工确认
- 问题：
  - 需要一轮更严格的启动、关闭、重启和多次重复启动验证

### 14. 状态持久化稳定

- 结果：`通过`
- 观察：
  - 当前状态文件持续可读
  - 旧档兼容与 `firstBondAt` 迁移已补过修正
  - 目前未见旧宠物被误打回未认主状态
- 问题：
  - 还可再补一轮“损坏文件时如何回退”的验证

### 15. bridge 运行稳定

- 结果：`通过`
- 观察：
  - `codex_usage_bridge.py` 当前在运行
  - `codex-bridge-state.json` 已记录会话路径、偏移和总 token
  - 已有大量已消费事件 id，说明没有明显重复回放
- 问题：
  - 还缺一轮人工中断后恢复验证

## 七、发布配套

### 16. 文档是否足够支撑私测

- 结果：`通过`
- 观察：
  - 当前已具备：
    - `README.md`
    - `docs/v1-private-beta-quickstart.md`
    - `docs/v1-release-plan.md`
    - `docs/v1-acceptance-checklist.md`
    - `docs/v1-validation-template.md`
- 问题：
  - 还可以再做一张更短的“1 分钟上手图”

### 17. 已知限制是否清楚

- 结果：`通过`
- 观察：
  - 当前 README 已明确 `developer preview`
  - 已明确当前首发以 `Codex + macOS` 为主
- 问题：
  - 无

## 问题汇总

### P0

- 无

### P1

- 还缺一轮严格的单实例与启动链人工验证
- 拍准 / 拍偏 / 错过的强弱动作还可再拉开一档

### P2

- 首次诞生的迎接感还可更完整
- 图鉴里的关系痕迹表达还可更直观
- 还可补一张更短的私测上手图

## 下轮动作建议

1. 先做一轮严格的启动、关闭、重启、重复启动验证。
2. 再把拍准 / 拍偏 / 错过的强弱层级继续拉开。
3. 最后补一次首次诞生迎接感的收口。

## 是否建议发出本轮私测包

- 建议：`不发`
- 原因：
  - 主链路已经成立，但单实例和桌面可见反馈还需要再做一轮更严格的人工验收
  - 目前更像“高完成度可玩原型”，离“可发的私测版”还差最后一轮收口验证
